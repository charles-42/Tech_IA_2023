def nombre_voiture_par_marque(self,marque):
    nombre_voiture_marque_en_inventaire = 0
    for voiture in self.inventaire:
        if voiture.marque == marque_recherchee:
            nombre_voiture_marque_en_inventaire += 1


def ajouter_une_voiture(self,voiture):
    isinstance(voiture,Voiture)
    for voiture in self.inventaire:
        ajouter_une_voiture = inventaire.append(voiture)
     return ajouter_une_voiture


def retirer_une_voiture(self,marque):
    marque = 1
    retirer_ = marque.pop(Voiture)
    return retirer_